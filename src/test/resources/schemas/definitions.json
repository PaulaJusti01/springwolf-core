## 🐳 Docker

### 📄 Dockerfile
Arquivo responsável por definir a **imagem Docker** da aplicação no padrão BYOC.

É nele que são configurados, por exemplo:
- a **imagem base** (runtime),
- a instalação de **dependências** (ex.: `requirements.txt`),
- a cópia do código da aplicação para dentro do container,
- variáveis de ambiente e comandos de inicialização (`ENTRYPOINT`/`CMD`).

A imagem gerada a partir do `Dockerfile` é a base utilizada no processo de **build e publicação no ECR** pela pipeline do **IU Lotus** (quando `publish.ecr: true`).

> Observação: a localização do `Dockerfile` pode variar conforme o padrão do time (ex.: raiz do repositório ou dentro de `app/`), mas ele deve ser referenciado de forma compatível com o fluxo de build definido no `iupipes.yml`.

### 📄 .dockerignore (opcional)
Arquivo que define quais arquivos/pastas **não** devem ser copiados para o contexto do Docker build, reduzindo tamanho de imagem e tempo de build.
Exemplos comuns: `.git/`, `__pycache__/`, `.venv/`, `tests/`, arquivos locais e caches.
