# Converter para numpy e garantir que tudo é float
X_train_np = X_train.to_numpy().astype(float)
y_train_np = y_train.to_numpy().astype(float)
X_test_np = X_test.to_numpy().astype(float)
y_test_np = y_test.to_numpy().astype(float)

# Agora treina o modelo
pipeline = lotus.train_model(
    project_id,
    "simple_xgboost_clf",
    X_train_np,
    y_train_np,
    X_test_np,
    y_test_np,
    max_evals=2,
    verbose=False
)
