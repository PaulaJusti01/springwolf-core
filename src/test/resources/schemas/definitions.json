## 📁 app/

Diretório responsável pelo **código da aplicação** no padrão BYOC do IU Lotus.  
Aqui ficam a implementação da lógica de negócio, artefatos auxiliares, scripts, testes e a definição da imagem Docker.

---

### 📁 app/artifacts/

Contém **artefatos auxiliares da aplicação**, como arquivos intermediários, modelos, parâmetros ou recursos necessários durante a execução.

> O conteúdo desta pasta varia conforme a implementação da aplicação.

---

### 📁 app/server/

Diretório responsável pela **camada de serviço** da aplicação.

Normalmente contém:
- inicialização do servidor,
- definição de endpoints,
- configuração de frameworks (ex.: FastAPI, Flask),
- integração entre a aplicação e o runtime de execução.

---

### 📁 app/shell-scripts/

Scripts auxiliares em **shell** utilizados durante build, deploy ou execução da aplicação.

Exemplos comuns:
- scripts de inicialização,
- validações de ambiente,
- ajustes de permissões,
- comandos customizados do container.

---

### 📁 app/src/

Diretório principal do **código-fonte da aplicação**.

Geralmente contém:
- lógica de negócio,
- classes e funções principais,
- processamento de dados,
- carregamento e execução de modelos.

---

### 📁 app/tests/

Contém os **testes automatizados da aplicação**, como testes unitários e de integração.

Esses testes podem ser executados:
- localmente,
- durante o build,
- como parte dos gates de qualidade da esteira.

---

### 📄 app/Dockerfile

Arquivo responsável por definir a **imagem Docker da aplicação** no padrão BYOC.

Define, entre outros:
- imagem base (runtime),
- instalação de dependências,
- cópia do código para o container,
- comandos de inicialização da aplicação.

A imagem gerada a partir deste `Dockerfile` é utilizada no processo de **build e publicação no Amazon ECR** pela pipeline do IU Lotus.

---

### 📄 app/requirements.txt

Lista de **dependências Python da aplicação**.

Utilizado durante:
- o build da imagem Docker,
- a execução local,
- a execução de testes.

---

### 📄 app/sys-requirements.txt

Lista de **dependências de sistema operacional** necessárias para a aplicação.

Exemplos:
- bibliotecas nativas,
- pacotes do sistema,
- dependências exigidas por bibliotecas Python.

---

### 📄 app/tox.ini

Arquivo de configuração do **Tox**, utilizado para padronizar a execução de testes e validações locais.

Define:
- ambientes de teste,
- versões de Python,
- comandos de execução,
- integrações com linters e frameworks de teste.

---
