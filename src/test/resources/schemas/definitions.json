locals {
  tags_array = [
    for key, value in merge(
      var.tags,
      {
        Plataforma    = "iulotus"
        Experiment_id = var.experiment_id
        Model_name    = var.model_name
      }
    ) : {
      Key   = key
      Value = value
    }
  ]
}
